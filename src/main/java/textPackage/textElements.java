package textPackage;
/**
 * Base interface for all text components (words, punctuation, sentences).
 * Serves as a marker for grouping different structural parts of the text.
 */
public interface textElements {
    // Marker interface for text elements
}
